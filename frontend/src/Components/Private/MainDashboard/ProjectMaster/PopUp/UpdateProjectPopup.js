

export const UpadateProjectPopup = ({ id, name }) => {

    // console.log("Id:" + id + " Name:" + name);

    return (
        <span
            onClick={console.log("Idfdggd:" + id + " Name:" + name)}
            className="update">
            <i className="fa-solid fa-pen text-success cursor-pointer"></i>
          
        </span>
        
    )
}

